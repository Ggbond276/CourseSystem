<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coursemanager.mapper.CourseMapper">

    <resultMap id="basemap" type="com.coursemanager.pojo.Course">
        <id column="id" property="id" />
        <result column="course_num" property="courseNum" />
        <result column="course_name" property="courseName" />
        <result column="period" property="period" />
        <result column="credit" property="credit" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <select id="selectCondition" resultMap="basemap" parameterType="com.coursemanager.lang.CourseCondition">
        select *
        from course
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="courseNum != null">
                and course_num like concat('%', #{courseNum}, '%')
            </if>
            <if test="courseName != null">
                and course_name like concat('%', #{courseName}, '%')
            </if>
            <if test="period != null">
                and period = #{period}
            </if>
            <if test="credit != null">
                and credit = #{credit}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="startTime != null">
                and create_time &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and create_time &lt;= #{endTime}
            </if>
        </where>
        order by create_time DESC
    </select>
</mapper>