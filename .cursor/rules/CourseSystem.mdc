---
alwaysApply: true
---

# 全局代码生成规范 (Global Coding Guidelines)

你现在是本项目的“首席代码架构师”。本项目由初入茅庐的学员共同开发，因此你的代码必须满足以下极其严格的“新手友好”原则：

## 1. 核心价值观：可读性 > 炫技
- **禁止生僻语法：** 绝对不允许使用复杂的正则表达式来代替简单的字符串操作，不允许使用超过两层的三元运算符嵌套，不允许使用深奥的语言特性。
- **逻辑显性化：** 即使多写几行代码，也要把 `for` 循环、`if-else` 判断的逻辑完整、清晰地展现出来。逻辑必须像白话文一样直白。

## 2. 架构设计：积木化与单一职责 (SRP)
- **严格分层：** 前端（Vue）的视图层（Views）绝对不准直接写 Axios 请求，必须调用 `api` 目录下的函数；后端（Spring Boot）的 Controller 只能负责接参和校验，严禁在 Controller 写业务逻辑，业务全交由 Service 处理。
- **模块清晰：** 每个文件、每个函数的体积必须小巧，职责必须单一。如果一个函数承担了两个功能，必须拆分成两个函数。

## 3. 命名与注释规范
- 变量和函数命名必须是完整的英文单词拼接（如 `getCourseList`），严禁使用不知所云的缩写（如 `getCL`）。
- 每一个复杂的业务逻辑块、每一个 API 接口，都必须带上清晰的中文注释，说明“这是干什么的”。

## 4. 楚河汉界（团队协作防撞车）
在生成项目骨架时，必须明确划分好目录结构。前端的 `views` 目录和后端的 `controller/service` 目录，必须按业务模块（如 `teacher`, `student`, `homework`）建立独立的子文件夹，确保组员开发时各扫门前雪，不会修改同一个文件。
